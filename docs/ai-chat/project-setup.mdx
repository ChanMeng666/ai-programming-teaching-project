---
sidebar_position: 3
title: 项目创建
description: 创建和初始化AI对话系统项目
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 项目创建

## Hugging Face Space设置

<div className="space-setup">

### 创建新Space

<Tabs>
  <TabItem value="ui" label="使用界面" default>
    1. 登录Hugging Face
    2. 点击 "New Space"
    3. 填写基本信息：
    ```yaml
    Owner: ChanMeng666
    Space name: role-play-chat
    SDK: Gradio
    Hardware: CPU basic (2 vCPU, 16 GB)
    Visibility: Public
    ```
  </TabItem>
  <TabItem value="cli" label="使用命令行">
    ```bash
    # 使用huggingface_hub CLI创建space
    huggingface-cli repo create role-play-chat --type space
    ```
  </TabItem>
</Tabs>

:::tip 创建建议
- 选择合适的硬件配置
- 设置有意义的项目名称
- 考虑项目可见性
:::

</div>

## 本地项目配置

<div className="local-setup">

### 1. 克隆项目
```bash
# 克隆Space仓库
git clone https://huggingface.co/spaces/ChanMeng666/role-play-chat
cd role-play-chat

# 初始化项目
python -m venv venv
source venv/bin/activate  # Windows使用: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. 项目结构创建
```bash
mkdir -p {assets,utils,tests}
touch main.py character_profiles.py dialogue_system.py
```

### 3. Git配置验证
```bash
# 测试Git配置
git config --list

# 提交测试更改
git add .
git commit -m "Initial project setup"
git push
```

</div>

## 基础配置

<div className="basic-config">

### 依赖配置
```python
# requirements.txt
gradio>=3.50.2
huggingface-hub>=0.19.4
torch>=2.0.0
transformers>=4.35.2
python-dotenv>=1.0.0
```

### 环境变量设置
```bash
# .env
HUGGINGFACE_TOKEN=your_token_here
MODEL_PATH=your_model_path
LOG_LEVEL=INFO
```

:::caution 注意事项
- 不要将.env文件提交到版本控制
- 妥善保管访问令牌
- 在生产环境使用安全的方式管理密钥
:::

</div>

export const styles = {
  spaceSetup: {
    backgroundColor: '#f6f8fa',
    padding: '1rem',
    borderRadius: '8px'
  },
  localSetup: {
    borderLeft: '3px solid #1a73e8',
    paddingLeft: '1rem'
  },
  basicConfig: {
    backgroundColor: '#f0f7ff',
    padding: '1rem',
    borderRadius: '8px'
  }
}; 